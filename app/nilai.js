import { View, Text } from 'react-native';

export default function NilaiScreen() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>Halaman Nilai (Segera Hadir)</Text>
        </View>
    );
}